import CountyModel from '../constants/Model/CountyModel';

const CountyReducers = (state = CountyModel, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default CountyReducers;
